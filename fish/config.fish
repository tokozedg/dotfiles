# for better colors in nvim
set -x NVIM_TUI_ENABLE_TRUE_COLOR 1
# set default terminal to termite; used by i3-sensible-terminal
set -x TERMINAL termite
set -x EDITOR nvim
# don't show greeting
set fish_greeting
